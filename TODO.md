# Этот файл - заметки самого разработчика. Здесь он пишет свои мысли о текущем состоянии FIC, изменения которые нужно внести и т.п. Данные заметки не являются мотивацией к действию или ближайшими планами по функционалу

## Перевести всю локализацию на LocalizationManager (убрать хардкод русских/английских предложений в коде)

## Добавить больше логгирования (TRACE,DEBUG,INFO,WARN,ERROR,FATAL) во все функции/методы в программе

## Протестировать контроль устройств (QEMU/KVM + физический компьютер).

## Доработать контроль устройств (на физическое машине в текущей реализации игнорируется клавиатура и тачпад)
## Также матрица и аккумулятор имеют параметры для контроля. Надо подумать, нужно ли эти устройства контролировать.

## Нужно подумать, что делать с политиками, которые неприменимы для данной ОС. Вероятно, в класс политики придется добавить булевый параметр "политика_поддерживается_ОС?"

## Будущие модули: настройка браузеров

## Корректировки для политики SYSCTL_rp_filter_enable - нужно понять что с этим делать
## Политика ставит net.ipv4.conf.all.rp_filter и net.ipv4.conf.default.rp_filter, а NetworkManager может задать:
## net.ipv4.conf.enp5s0.rp_filter = 2 и net.ipv4.conf.wlan0.rp_filter
## Либо удалять (комментировать) настройки для отдельных интерфейсов, либо переопределять нашими strict-значениями
## Более корректное решение: Ввести модель зависит/рекомендуется для политик. Например, для политики "Включение IPv4 rp_filter"
## РЕКОМЕНДУЕТСЯ включить политику "Отключить MPTCP в NetworkManager" (пока такой политики нет)
## [connection.no-mptcp]
## connection.mptcp-flags=0x1

## Ввести для всех политик механизм realtime-отслеживания измененных системных файлов (+,возможно, предовращения изменения). В текущем виде FIC скорее программа периодического приведения системы в защищенное состояние, а не программа непрерывного поддержания защищенного состояния

## Надо протестировать, как PAM реагирует на бессмысленные правила (или правила, обходящие защитные проверки)
## auth    sufficient    pam_allow.so.1 - разрешает вход и останавливается здесь (не идёт дальше)
## auth requisite pam_faillock.so preauth silent audit deny=5 unlock_time=300 - наши супер-безопасные настройки

## Все политики не имеют rollback. Т.е. у администратора нет способа убрать все изменения, которые внёс fic (если только не начать их ему вручную вичищать)
